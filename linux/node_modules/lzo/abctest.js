const BUF_SIZE = parseInt(process.env.BUF_SIZE, 10);

const lzo = require('.');
const str = new Array(BUF_SIZE).fill('x').join('');
const comp = lzo.compress(str);

console.log(comp);
console.log(lzo.decompress(comp, 1));
